# When Bradbury data is available, override with:
#   uv run python -m solar_seg.train data.data_root=data/processed/bradbury
defaults:
  - data: bdappv
  - model: mask2former_swin_b
  - trainer: base
  - _self_

seed: 42
experiment_name: combined_bdappv_bradbury
mlflow_tracking_uri: mlruns
